executor:
  basic:
    coreSize: 2
    maxQueueSize: 5000
    queuePolicy: AbortPolicy

logbus:
  filePath: "target/logbus"
  token: "dwe"
  split-org: 200
  gzip: true
  post-url: "http://localhost:8888/log/push/List"
